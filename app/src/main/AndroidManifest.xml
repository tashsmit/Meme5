<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="meme5.c4q.nyc.meme_project" >
    <uses-feature android:name="android.hardware.camera"
        android:required="false" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:largeHeap="true">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".ChooseMemeStyle"
            android:label="@string/title_activity_choose_meme_style" />

        <activity
            android:name=".VanillaMeme"
            android:label="@string/title_activity_vanilla_meme"
            android:noHistory="true"/>

        <activity
            android:name=".DemotivationalMeme"
            android:label="@string/title_activity_demotivational_meme"
            android:noHistory="true"/>

        <activity
            android:name=".SplashScreen"
            android:label="@string/title_activity_splash_screen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MemeSearch"
            android:label="Google"
            android:noHistory="true"/>

    </application>

</manifest>
